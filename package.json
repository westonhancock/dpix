{
  "name": "dpix-suite",
  "version": "1.1.1",
  "description": "dpix - Image compression and optimization for everyone",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:cli": "cd packages/cli && npm install",
    "install:electron": "cd packages/electron && npm install",
    "install:all": "npm run install:cli && npm run install:electron",
    "build:binary": "node installer/build-binary.js",
    "build:electron": "cd packages/electron && npm run build",
    "build:dmg": "node installer/build-dmg.js",
    "build:all": "npm run build:binary && npm run build:electron && npm run build:dmg"
  },
  "author": "Weston Hancock <weston.e.hancock@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "create-dmg": "^7.1.0",
    "pkg": "^5.8.1"
  }
}
